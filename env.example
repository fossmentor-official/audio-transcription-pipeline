# Copy to .env.loc and adjust for your machine:
#   cp env.example .env.loc

FLASK_SECRET_KEY=dev-key
MAX_CONTENT_LENGTH_MB=100
MAX_FILES=10
MAX_WORKERS=2
UPLOAD_DIR=uploads
ALLOWED_EXTENSIONS=.wav,.mp3

# tiny = faster on CPU; turbo = better accuracy
WHISPER_MODEL=tiny
